% Additional parameters, which must be separated by commas,
% are included in square brackets ([...])
% and used to configure this instance of the article class;
% i.e., settings we wish to use for this particular article-class-based document.
% 12pt sets the font size
% letterpaper sets the paper size
% the default font size is 10pt
% As for the paper size, other possible values are a4paper and legalpaper.
\documentclass[12pt, letterpaper]{article}
% loading an external package (here, graphicx) to extend LATEX’s capabilities,
% enabling it to import external graphics files.
\usepackage{graphicx}
